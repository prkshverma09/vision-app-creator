# C0 canonical contract fixtures

`models.json` contains one valid and one intentionally invalid payload per authoritative Pydantic model. Discriminated-union examples are also published in `packages/contracts/examples`.
